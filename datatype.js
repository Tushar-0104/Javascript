"use strict";  // treat whole JS code as newer version

// alert(3+3) we are using node js not browser.Their is different syntax to use alert in node js.

let number = 141
let name = "tannu"
let city;
let login=true

console.log(typeof number);
console.table([typeof number, typeof name, typeof city, typeof login])
